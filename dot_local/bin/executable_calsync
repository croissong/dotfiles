#!/usr/bin/env bash
set -e

davmail &
davmailPID=$!
while ! ncat -z localhost 1080; do
  sleep 0.5
done

if [ $1 = "fix" ]; then
   vdirsyncer discover calendar_wrk_dw
else
  vdirsyncer sync
fi

khal list

kill $davmailPID
