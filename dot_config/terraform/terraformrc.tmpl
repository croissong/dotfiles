credentials "gitlab.devops.telekom.de" {
  token = "{{ (secretJSON "-d" "--output-type=json" (print .chezmoi.sourceDir "/secrets.yml")).wrk.svh.gitlabToken }}"
}

credentials "app.terraform.io" {
  token = "{{ (secretJSON "-d" "--output-type=json" (print .chezmoi.sourceDir "/secrets.yml")).terraformToken }}"
}
