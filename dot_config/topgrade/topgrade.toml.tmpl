[misc]
only = ["krew", "sheldon", "firmware", "custom_commands"] # "chezmoi",

skip_notify = true

[firmware]
upgrade = true

[commands]
buku = "$DOT/dotfiles/dot_config/topgrade/buku.fish"

krew_export = "$XDG_CONFIG_HOME/topgrade/krew-export.sh"

the_way_export = "$XDG_CONFIG_HOME/topgrade/the_way_export.sh"

gh_stars_export = "$XDG_CONFIG_HOME/topgrade/gh-stars-export.sh"
# "xdg-ninja" = "xdg-ninja"
